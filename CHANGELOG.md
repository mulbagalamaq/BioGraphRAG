# Changelog

## v0.1.0
- Initial public release
- Graph-native RAG with Neptune expansion and PyG fusion
- Vector store: OpenSearch (default) and optional local FAISS backend
- Minimal CI (lint + tests) and pre-commit hooks

